---
title: Mongez
description: Production TypeScript packages for state, routing, forms, networking, configuration, encryption, and more — 24 focused packages, designed to read equally well to humans and AI coding agents.
template: splash
hero:
  tagline: Ship features, not boilerplate. 24 production TypeScript packages, fluent in human and AI.
  image:
    alt: "Mongez — fire-and-ice lightning bolt logo"
    html: |
      <img src="/mongez.png" alt="Mongez" class="mongez-hero-logo" width="260" height="260" />
  actions:
    - text: Explore the ecosystem
      link: /ecosystem/
      icon: right-arrow
      variant: primary
    - text: Which package?
      link: /which/
      icon: right-arrow
      variant: secondary
    - text: View on GitHub
      link: https://github.com/hassanzohdy
      icon: external
      variant: minimal
---

import { Card, CardGrid } from "@astrojs/starlight/components";
import PackageCard from "../../components/PackageCard.astro";
import { FEATURED_PACKAGES } from "../../data/packages";

## What is Mongez?

Mongez is a production toolkit for TypeScript apps: **24 focused packages** spanning state, routing, forms, networking, configuration, encryption, internationalisation, and build tooling. Each does one thing well, ships with type definitions plus AI-readable docs (`llms.txt`, `skills/`), and works standalone — or composes with the rest of the family when you want the whole stack.

No wrappers. No magic globals. No runtime bloat. Just typed packages that get out of your way in production.

## The name

**Mongez** ‹*mon-jez*› · مونجز

In Arabic, *mongez* describes someone who **gets it done, fast** — literally a "rabbit accelerator." Effectively, the friend who pushes a feature to prod while you're still drafting the function signature. The toolkit is named in their honor.

## Featured packages

The five most-reached-for `@mongez/*` packages. Start here if you're not sure which one fits.

<div class="mongez-pkg-grid mongez-pkg-grid--homepage">
  {FEATURED_PACKAGES.map(pkg => <PackageCard pkg={pkg} />)}
</div>

[See all 24 packages →](/packages/)

## Pick your starting point

<CardGrid>
  <Card title="Foundations" icon="seti:json">
    The non-React utility belt every other package builds on — type guards, ~130 utility functions, an event bus, a chainable collection wrapper.

    [`@mongez/events`](/events/overview/) · [`@mongez/reinforcements`](/reinforcements/overview/) · [`@mongez/supportive-is`](/supportive-is/overview/) · [`@mongez/collection`](/collection/overview/)
  </Card>
  <Card title="Core libraries" icon="seti:typescript">
    Framework-agnostic primitives: state, HTTP, caching, configuration, encryption, DOM, URLs, i18n. Each one usable standalone in Node, browsers, or Vue / Svelte / vanilla TS.

    [`@mongez/atom`](/atom/overview/) · [`@mongez/http`](/http/overview/) · [`@mongez/cache`](/cache/overview/) · [`@mongez/config`](/config/overview/) · [`@mongez/dotenv`](/dotenv/overview/) · [`@mongez/encryption`](/encryption/overview/) · [`@mongez/dom`](/dom/overview/) · [`@mongez/concat-route`](/concat-route/overview/) · [`@mongez/query-string`](/query-string/overview/) · [`@mongez/localization`](/localization/overview/)
  </Card>
  <Card title="React ecosystem" icon="seti:react">
    React adapters and React-first packages — atoms with hooks, server-data cache, configuration-based router, form primitives, document `<head>` management, auth state.

    [`@mongez/react-atom`](/react-atom/overview/) · [`@mongez/atomic-query`](/atomic-query/overview/) · [`@mongez/react-router`](/react-router/overview/) · [`@mongez/react-form`](/react-form/overview/) · [`@mongez/react-helmet`](/react-helmet/overview/) · [`@mongez/react-localization`](/react-localization/overview/) · [`@mongez/user`](/user/overview/)
  </Card>
  <Card title="Build & AI tooling" icon="seti:config">
    Standalone tools for shipping TS packages and aligning every coding agent on your team — Vite for SPAs, pkgist for builds & releases, agent-kit for AGENTS.md-driven config.

    [`@mongez/vite`](/vite/overview/) · [`@mongez/pkgist`](/pkgist/overview/) · [`@mongez/agent-kit`](/agent-kit/overview/)
  </Card>
</CardGrid>

## Package independence

Every package can be used standalone. `@mongez/reinforcements` is just a utility belt — no peers. `@mongez/atom` is framework-agnostic and works in Vue or plain TypeScript. The React adapters only pull in what they need. `@mongez/pkgist` is a CLI tool you can install in *any* TypeScript project — Mongez or otherwise.

When you adopt one package, the others compose naturally — but nothing forces you to take all of them.

## Built for the agent era

Every package ships with `llms.txt`, `llms-full.txt`, and a `skills/` folder of dense reference cards designed for LLM ingestion. Claude, Cursor, Copilot, Codex — they all get the API shape right on the first try.

And [`@mongez/agent-kit`](/agent-kit/overview/) takes it further: write your project's AGENTS.md once, and every coding agent (Claude Code, Cursor, Copilot, Aider, Codex, Amp, …) reads the right config from the right path automatically.

[See the ecosystem `llms.txt` →](https://mongez.js.org/llms.txt) · [Full reference (`llms-full.txt`) →](https://mongez.js.org/llms-full.txt)
